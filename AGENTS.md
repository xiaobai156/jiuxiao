# 灵蛇九肖_修复版v2 — 项目规则

继承父目录 `C:\Users\Administrator\Desktop\每天工具\爬虫合集\AGENTS.md`。本文件只规定本项目差异；父目录规则未重复的部分继续适用。

## 项目边界与固定路径

- 项目根目录：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\灵蛇九肖_修复版v2`。
- 活跃站点：`config\sources.json`。
- 封存站点：`config\archived_sources.json`。
- 动态主列表解析覆盖：`config\main_list_parser_overrides.json`；按“目录名+栏目标题+完整URL”绑定，不改变方向。
- 正式近10期缓存：`cache\recent_10_cache.json`。
- 单期成功报告：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳\<期数>期-生肖.txt`。
- 单期失败报告：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳失败\<期数>期-生肖-失败.txt`。
- 多期全失败汇总：`outputs\<起始期>-<结束期>-all-failures.txt`。
- 正式入口：`run_v2.py -> __main__.py`，公开 `crawl`、`crawl-range`、`shadow`、`preflight`、`switch`、`rollback`。
- `cli.py` 的 `onboard`、`verify`、`duplicate` 仅是未装配接口；没有接入正式入口和验收前，不得称为正式命令。

## 执行范围与回复

1. 用户明确了项目、站点/URL、期数、方向和操作后，已授权范围内的工作应直接完成；只有缺少会实质改变结果的信息，或下一步超出授权范围时才询问。
2. 用户说“开始”后，只处理已确认范围。不得因单站任务顺手跑全站、其他期数或其他项目。
3. 抓取、测试、配置、缓存、报告和封存分别视为独立操作；用户授权其中一项，不自动授权其他会改变结果的操作。
4. 删除、封存、恢复、正式切换、覆盖输出或缓存必须由用户明确授权。诊断或审计本身不授权修复。
5. 回复优先采用“结论 → 编号步骤 → 下一步”。`i-have-adhd` 可用时按其技能执行；不可用时不阻塞工作，也不得声称已加载该技能。
6. 代码修改遵循 `ponytail full`：先读真实调用链，优先复用现有代码和标准库，避免无明确收益的抽象、依赖和防御性架构。该偏好适用于代码修改，不要求数据维护任务套用复杂开发流程。

## 九肖数据契约

- 成功结果必须恰好包含9个互不重复的合法生肖，并保留网页原始顺序。
- 期数行、九肖原文、目录/作者/栏目锚点和区块边界必须属于同一来源文档和同一目标区块。
- 页面只有购买、锁定、`00准` 或其他提示而没有真实九肖/分组原文时，失败。
- 站点身份由目录名、规范化URL/topic/文章ID、`section_marker`、方向和实际解析字段共同确定；不能用列表下标或单独URL代替。

## 方向与解析

固定流程是：锁定权威文档和目标区块 → 收集区块内全部候选 → 校验期数、语义、数量和合法值 → 按配置窗口应用 `top`/`bottom` → 检查指定期和同期唯一性。

- `position` 只接受 `top`、`bottom` 及项目已有的中文等价写法，并保持用户原意。
- 本项目默认只在目标区块该方向最近3个高可信具体期号中寻找指定期；窗口不得运行时扩大或换方向。当前期由该目录方向的首个有效记录定义，不能取全页最大期号。
- 先命中目录名、作者名、栏目名或 `section_marker`，再解析该目录的方向区间；锚点缺失、多重冲突或区块无法唯一确定时失败，不得退回整页扫描。
- 同页的三肖、四季、风雨雷电、推荐、广告、上一篇/下一篇和相邻历史块必须由专属解析或 `section_marker` 排除。
- 普通九肖、跨行、分组、动态文章、列表详情、用户主页和OCR版式必须使用已验收的对应解析器；专属解析失败不得用其他解析器凑成功。
- OCR、iframe、script、API 和列表详情只有在该站点配置明确授权、且证据仍通过统一校验时才能参与解析；不得拼接不同文档的锚点和数据。
- 分组型必须从目标正文原文转换。站点 `group_map` 是组内生肖顺序的唯一权威；证据至少包含原文行、分组类型、分组字、完整映射和转换后的九肖。

## 动态文章

- 配置有 `api_url` 时优先请求；没有时只使用项目已经实现的同文章ID接口规则。
- 接口正文必须同时证明目标文章ID、目录/栏目、期数、九肖和区块边界。404或明确空壳时，才可按项目已实现的浏览器策略渲染；其他接口异常不得随意浏览器兜底。
- 渲染后仍须通过同文章ID、锚点、方向、指定期、九肖数量和边界校验。不能把整页聚合内容交给通用正则。

## 新增、修复与封存

### 新增站点

用户明确“开始”后，按以下顺序处理单个指定站点：

1. 检查精确同名、规范化同URL/topic/文章ID、字段、方向、相似标题/作者/栏目和同页多目录。
2. 真实访问来源，确认锚点、区块、方向窗口、期数和九肖格式。
3. 用专属解析取得有效最新期或上一期，并按项目正式判重器比较当前有效近10期缓存的共同期数。
4. 完成必要的解析验收后，原子写入配置并回读身份、字段、方向和顺序。

普通准入需要10期有效历史。用户点名并再次说“开始”可仅放宽不足10期或3至5期疑似重复；不得放宽无有效期、方向/栏目不明、同期冲突或连续6期重复。

### 失败修复

“开始验证”只做指定站点的真实复现和隔离验证，不改正式配置、缓存或报告；验证目标期、相邻/不存在期以及与本次失败直接相关的边界情况。

只有用户随后明确“开始正式更新”，才迁移已验证的最小改动，并重新运行该站点的完整单期流程，检查报告、缓存和相关测试。

### 封存与恢复

- 封存只处理用户点名的站点：移出活跃配置、写入封存配置，并移除该站点的精确缓存身份；不抓取、不写成功报告、不影响同URL其他栏目。
- 用户明确授权封存完成后，若当期失败报告存在该站点记录，只删除该站点记录，保留其他记录及其顺序。
- 恢复必须由用户点名授权；不得因新增、修复或读取失败报告自动恢复封存站点。

## 缓存与报告

- 单期实时判定只使用本次抓取的原始文档和解析证据；缓存只在结果定稿后用于近10期滚动，或在新增站点正式判重时按共同期数读取。
- 单期失败记录本期错误状态；若本期已有旧值，必须移除该期旧值，不能用旧值冒充成功。失败不得删除其他期历史。
- 多期任务逐期生成既有成功/失败报告，但不更新正式缓存。
- 缓存更新必须保持其他站点的身份、URL/topic、方向、`section_marker`、原始顺序和历史窗口；使用现有锁、同目录临时文件和原子替换。
- 成功报告只写完整校验通过的数据；失败报告至少包含站点、URL、方向、期数、失败阶段和原因。
- 成功/失败报告沿用现有命名。新增或修复的定向任务只更新指定站点对应记录；失败TXT按项目既定规则处理，不得覆盖其他站点记录。

## 判重

- 只比较相同具体期号；双方没有共同有效期、候选不可信或缓存失效时，判重未完成，不能输出“无重复”。
- 近10期总命中1至2期不判重复；3至5期标记疑似重复并暂停普通准入；6期及以上禁止加入。最长连续相同期数只能辅助说明，不能替代总命中口径。
- 单期实时结果与已有缓存不同不构成 `CANDIDATE_CONFLICT`；缓存不得改判本次实时结果。

- 失败重抓命令只读取指定期数的失败 TXT；仅重抓其中能按完整 URL 唯一匹配的活跃站点，禁止扩大为全站。
- 重抓成功时只清除对应失败行、更新对应成功结果和站点缓存；重抓失败时保留对应失败记录，其他站点、缓存和 TXT 内容不受影响。

## GitHub 同步规则

- 当前项目的 GitHub `origin` 为 `https://github.com/xiaobai156/jiuxiao.git`。
- 本项目每次代码、配置或文档修改在必要验证通过后，自动提交并推送到 `origin`。
- 自动提交只包含本次修改；必须保留用户已有未提交改动，不得擅自覆盖、重置、清理或代提交。
- 提交前必须检查暂存文件；不得提交或推送凭据、密钥、虚拟环境、缓存、临时文件、编译产物、日志或无关项目。
- 验证未通过、远端冲突、认证失败或无法安全区分既有改动时，停止推送并报告具体原因。
