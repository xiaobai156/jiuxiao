# 灵蛇九肖_修复版v2 项目专属规则

本项目继承上级 `..\AGENTS.md`。本文件只定义灵蛇九肖 v2 的项目差异；未写事项按上级统一规则执行。

## 1. 项目边界与正式文件

- 生产项目路径：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\灵蛇九肖_修复版v2`。
- 活跃站点配置：`config\sources.json`。
- 动态主列表专属解析覆盖：`config\main_list_parser_overrides.json`；按“目录名+栏目标题+完整URL”绑定，不改变方向。
- 封存站点配置：`config\archived_sources.json`。
- 正式近10期缓存：`cache\recent_10_cache.json`。
- 单期成功输出：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳\<期数>期-生肖.txt`。
- 单期失败输出：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳失败\<期数>期-生肖-失败.txt`。
- 多期全失败汇总：`outputs\<起始期>-<结束期>-all-failures.txt`。
- 正式BAT：`爬虫-爬取灵蛇生肖-V2.bat`、`爬虫-多期抓取灵蛇生肖-V2.bat`、`影子运行-V1-V2.bat`、`一键回滚V1.bat`。
- `run_v2.py -> __main__.py` 的当前正式入口只公开 `crawl`、`crawl-range`、`shadow`、`preflight`、`switch`、`rollback`。
- `cli.py` 另定义 `onboard`、`verify`、`duplicate` 服务接口；未经正式运行时装配和验收，不得冒充 `run_v2.py` 已公开命令。

## 2. 九肖数据契约

- 每条成功结果必须恰好包含9个互不重复的合法生肖。
- 只允许：`鼠牛虎兔龙蛇马羊猴鸡狗猪`。
- 必须保留网页原始顺序，禁止排序、转为集合或用另一顺序补齐。
- 期数行和九肖原文必须属于同一目录/作者/栏目区块。
- 页面显示购买、锁定或 `00准`，但没有真实九肖或分组原文时，必须失败。

## 3. 方向与最近3期窗口

- `position` 只接受 `top` / `bottom` / `顶部` / `尾部` / `上` / `下`，必须保持用户原意。
- 固定方向后，只允许在目标区块该方向最近3个高可信的具体期号中寻找指定期。
- 当前期由该目录自身方向的首个有效记录定义；不得使用全页最大期号。
- 找不到指定期必须失败，不得换方向、扩大到15条或扫描整页。
- 历史数据按具体期号匹配，禁止按“第几条记录”硬对齐。
- 方向预登记只是固定规则，不是运行时自动换方向的许可。

## 4. 站点身份与同页多栏目

- 站点身份按“目录名 + 规范化URL/topic/文章ID + `section_marker` + 方向 + 实际解析字段”识别，不得按列表下标或单独URL识别。
- 新增前必须检查精确同名、标准化同URL/topic、同字段、相似标题/作者/栏目和同页多目录。
- 同一页面多个独立栏目只有在各自拥有不同且可验证的 `section_marker`、专属解析和独立缓存身份时才允许共存。
- 同一URL的不同栏目不得因为页面只请求一次而合并结果，也不得用后一栏目覆盖前一栏目。
- 候选站在全部硬条件通过前只能存在于临时对象，不得提前写入 `config\sources.json`。

## 5. 新增站点项目门禁

1. 读取活跃配置、封存配置、正式近10期缓存和现有解析注册表。
2. 先做同名、同URL/topic/文章ID、同字段和相似目录检查。
3. 真实访问候选来源，确定目录锚点、栏目锚点、方向、期数窗口、九肖格式和结束边界。
4. 按用户指定方向命中有效缓存的最新期或上一期中的至少一期。
5. 用同一套专属解析取得10个具体期号的有效九肖，每期都必须有栏目、方向、原文和边界证据。
6. 使用项目正式重复检测器和 `cache\recent_10_cache.json` 执行判重。
7. 完成专属解析验收，并记录来源、锚点、方向、期数、原文、数量、边界和历史记录。
8. 无硬拦截、无未解决疑似重复且专属解析通过后，才能原子写入正式活跃配置，并回读核对身份、字段和顺序。

- 候选页面全文最大期号、其他栏目最大期号和非目标方向期号不能作为基准。
- 一次全站检测需确定共同期数时，报告必须说明多数站在自身方向的首个有效期号依据；票数并列时停止猜测。
- 不足10期或需要疑似重复放行时，必须由用户对单个点名目录明确授权并再次说“开始”。特例只放宽获准事项，不放宽其他硬规则。

## 6. 专属解析边界

- 必须先命中该目录自己的目录名、作者名、栏目名或 `section_marker`，再进入该目录方向区间。
- 锚点缺失、多重冲突或无法证明目标区块时直接失败，禁止退回整页扫描。
- 同页九肖、三肖、四季、风雨雷电、推荐、上一篇/下一篇、广告和相邻历史块必须由 `section_marker` 或专属解析锁死，并在目标块结束边界停止。
- 只能从目标区块提取目标期数；相邻楼层、广告、历史尾巴和任意生肖串不得补数。
- 普通、跨行、分组、动态文章、列表详情、用户主页、OCR和其他特殊版式必须选择已验收的对应解析器；不得相互兜底。
- 解析证据必须保存实际期数列表、九肖原文、目录/栏目锚点、方向、区块边界、来源方式和失败原因。

## 7. 截图驱动规则

- 用户提供截图是为了定义真实版式的专属解析，不是仅证明“页面有数据”。
- 必须从截图识别目录/栏目标题、期数位置、顶部/尾部区域、九肖行或节点、数量和结束边界，并转换为代码级规则。
- 截图显示分行、嵌套、作者栏、接口字段或多区块时，必须证明它们属于同一目标区块。
- 截图与实时网页不一致时，必须报告差异并暂停通过。
- 截图不能代替真实访问、基准期验证或原文证据。
- 验收报告必须写清截图结构、专属解析方式、命中锚点、方向、期数、九肖边界和原文证据。

## 8. 分组型目录

只有真实目标正文命中分组语义时才按分组型处理：

- 分组类型：`琴棋书画`、`春夏秋冬`、`风雨雷电`。
- 对应括号分组字示例：`【琴书画】`、`【秋冬夏】`、`【风雷电】`。
- 已定义语义映射：`风=虎兔龙`、`雨=蛇羊马`、`雷=猴狗鸡`、`电=鼠牛猪`；`琴=兔蛇鸡`、`棋=鼠牛狗`、`书=虎龙马`、`画=羊猴猪`；`春=虎兔龙`、`夏=蛇马羊`、`秋=猴鸡狗`、`冬=鼠牛猪`。
- 仅整页其他位置出现分组词，不得把普通九肖目录误判为分组型。
- 分组型禁止普通九肖兜底，必须从目标正文原文分组字转换。
- 分组字必须属于同一分组类别并能组成有效9肖；缺失、跨类别、数量不足或转换不一致时失败。
- 每个站点配置中的 `group_map` 是组内生肖原始顺序的唯一权威，不得被全局默认映射覆盖。
- Evidence 必须同时保存目标栏目原文行、分组类型、原文分组字、完整 `group_mapping` 和转换后的九肖。
- 判重前先审计分组证据；缺原文、缺分组字、锚点不一致或转换不一致的期数标记为来源不可信，不参与判重。

## 9. 动态文章项目规则

对URL含 `/article/admin/` 的动态站：

1. 配置有 `api_url` 时优先请求；没有时只能按已实现的项目规则生成接口。
2. 接口成功且目标记录正文具有锚点、期数和九肖时，使用接口正文。
3. 接口HTTP 404时可转页面请求，必要时进入浏览器渲染。
4. 页面HTTP 200但只有空壳，或缺少指定期、目标锚点或真实九肖时，才可渲染。
5. 非404接口异常、无HTTP状态或网络失败不得随意渲染凑数；除非项目代码明确定义为可恢复重试。
6. 渲染后仍必须命中原目录/栏目/`section_marker`、指定期、用户方向、9肖数量和区块边界。
7. 接口、原始页面、浏览器三层的成功或失败来源必须进入验收报告。

- `/article/manager/`、`/article/lottery/` 等其他动态路径仍必须执行上级统一的文章ID唯一性、同记录字段和来源兜底边界。项目未明确实现对应API/浏览器策略时，只允许使用已注册且能证明同一文章ID的专属来源；否则失败，不得回退整页扫描或擅自宣称支持浏览器兜底。

## 10. 灵蛇九肖判重口径

本项目使用比上级“连续期”更严格的“近10期相同具体期号总命中数”口径：

- 比较值是“具体期号 + 完整9肖字符串 + 网页原始顺序”。
- 按双方共同具体期号对齐；生肖集合相同但顺序不同，不算同值。
- 近10期总命中1至2期：不判重复。
- 近10期总命中3至5期：疑似重复，报告对照目录并等待人工确认。
- 近10期总命中6期或以上：判定重复，默认拒绝新增。
- “最长连续相同期数”只是辅助信息，不得代替总命中数口径。
- 旧实时指纹脚本只能辅助排查，最终结论只来自项目正式重复检测器和 `cache\recent_10_cache.json`。
- 候选为空、缓存过期、来源不可信、期数不明或比较数据无证据时，判重无效，不得输出“无重复”。
- 用户对点名重复目录的特例授权只对当次有效，不改变后续新增的默认拒绝规则。

## 11. 缓存与每日同步差异

- `cache\recent_10_cache.json` 不得只保存九肖字符串；分组型目录必须带完整审计证据。
- 同步前后必须保持 `sources` 的目录原始顺序、身份、URL/topic、方向和 `section_marker`。
- 失败、轮空、补缺、排序或重抓不得使后续目录前移或改变位置。
- 同一目录当天只更新自己的期号和记录；不得按列表下标把其他目录写入该位置。
- 失败或缺失只记录该目录自身的异常，不得静默沿用旧成功结果掩盖本期失败。
- 单期实时结果与已有缓存不同时，不得转为 `CANDIDATE_CONFLICT`；单期结果只由本次实时抓取、解析和统一校验决定。缓存只能在结果定稿后滚动写入，新增站点正式判重时才读取。
- 单次抓取失败不得删除其他期已存在历史；本期若已有旧成功值，必须移除该期旧值，只记录本期错误状态。
- 多期抓取不得更新正式缓存；只生成每期原有成功/失败TXT和“多期全失败”汇总。
- 缓存不存在、过期、期数窗口不对、来源审计不可信或无原始记录时，必须停止给出新增判重结论。

## 12. 异常报告与删除边界

- 异常清单至少列出：目录名、URL/topic、方向、目标期、来源方式、锚点、成功/失败状态、实际期数、失败原因、来源可信状态和同页冲突。
- 连接、SSL、接口、渲染、目录缺失、空壳、购买锁定、当前期未更新和解析冲突必须逐站报告。
- 站点失败不删除，缓存失败不自动重建。
- 删除只处理用户点名的目录及其精确缓存身份，不得顺便删除同URL的其他栏目。
- 覆盖重跑产生的失败TXT不等于删除站点配置或历史缓存，报告必须区分。

## 13. 失败站点独立验证

- 用户提供失败站、URL或失败TXT后，只将点名站点加入独立验证清单；不得自动扩大到全站。
- 每个站点必须固定用户指定的目标期，不得从网页推断。
- 试验解析先留在独立验证注册表，未通过前不得修改正式解析器、正式配置、缓存或TXT。
- 独立验证必须检查API优先、原始页面、浏览器、script/iframe、标题正文分离、跨行数据、旧文档/导航干扰、方向最近3期和严格区块边界。
- 每站单独输出：是否命中指定期、实际九肖、方向、栏目锚点、数据语义、九肖数量、来源证据、重复生肖、同期冲突和具体失败原因。
- 独立验证不得调用正式缓存同步，不得覆盖正式TXT。
- 独立验证完整通过后，还必须等待用户的正式更新授权；只同步最小已验证改动。
- 正式同步后重新运行 `爬虫-爬取灵蛇生肖-V2.bat`，以新生成的正式成功/失败TXT和缓存作为最终验收。

## 14. 验收报告最低内容

每次新增、修复、手动抓取或缓存同步结束时，报告至少说明：

- 实际使用的V2路径和未触碰的旧目录。
- 每个目录命中的锚点、`section_marker`、方向、期数窗口和解析方法。
- 动态站使用接口、原始页面还是浏览器，以及每层的成功或失败原因。
- 实际抓到的具体期数、九肖原文、数量/边界校验结果；分组型还要报告分组原文和转换结果。
- 判重基准缓存、检测期数的确定依据、共同具体期号和总命中数。
- 通过、疑似重复、重复、无效候选、轮空、失败和未处理数量。
- 是否写入配置或缓存，是否保持站点身份和来源原始顺序。
- 所有未完成项和失败原因，不得省略。

## 15. 项目级验收

- 修复先增加可复现测试，再运行相关单元/集成测试和项目现有完整测试。
- 至少验证：方向不串、锚点缺失失败、同页多栏目隔离、分组证据、动态接口兜底、缓存顺序和多期不写缓存。
- 必须真实执行完整的手动指定期流程，不能只验证正则、单个函数或模拟文本。
- 完整真实验证必须检查API、script/iframe多文档、标题正文分离、跨行九肖、旧文档/导航冲突、同期冲突和需要时的浏览器兜底。
- 只有真实指定期成功、冲突判断正确、来源证据完整、缓存按原始位置写入且全部测试通过，才能报告“修复完成”。
- 缺少任一验证只能报告“部分验证”或“未完成”。
- 文档分发任务只检查Markdown结构、路径和规则差异；不得因文档整理修改Python、JSON、BAT、缓存或TXT。

## GitHub 同步规则

- 当前项目的 GitHub `origin` 为 `https://github.com/xiaobai156/jiuxiao.git`。
- 本项目每次代码、配置或文档修改在必要验证通过后，自动提交并推送到 `origin`。
- 自动提交只包含本次修改；必须保留用户已有未提交改动，不得擅自覆盖、重置、清理或代提交。
- 提交前必须检查暂存文件；不得提交或推送凭据、密钥、虚拟环境、缓存、临时文件、编译产物、日志或无关项目。
- 验证未通过、远端冲突、认证失败或无法安全区分既有改动时，停止推送并报告具体原因。
